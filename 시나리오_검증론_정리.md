# 시나리오 검증론 정리

## 📊 현재 구현된 검증론

### 1. **Stress Testing (스트레스 테스트)**
**위치**: `src/validation/run_stress_validation.py`

**내용**:
- 7개 스트레스 시나리오 검증
  - Scenario_A_Stagflation (스태그플레이션)
  - Scenario_B_Correlation_Breakdown (상관관계 붕괴)
  - Scenario_COVID19 (팬데믹)
  - Scenario_Tail_Risk (꼬리 위험)
  - Scenario_C_Interest_Rate_Shock (금리 쇼크)
  - Scenario_D_Swap_Point_Extreme (스왑포인트 극단)
  - Scenario_E_Regime_Transition (Regime 전이)

**지표**:
- 평균/최소/최대 K-ICS
- 100% 미만 일수 및 비율
- 150% 미만 일수 및 비율

**목적**: 극단적 시장 상황에서 K-ICS 방어력 확인

---

### 2. **Walk-Forward Validation (롤링 윈도우 검증)**
**위치**: `src/validation/enhanced_validation.py` → `walk_forward_validation()`

**내용**:
- 시간 순서를 유지한 롤링 윈도우 방식
- Look-ahead bias 방지
- 윈도우 크기: 252일(1년), 504일(2년), 756일(3년)
- 스텝 크기: 63일(3개월), 126일(6개월)

**지표**:
- 각 윈도우별 Train/Test SCR 평균 및 표준편차

**목적**: 시계열 데이터에서 미래 정보 누수 방지

---

### 3. **Bootstrap Validation (부트스트랩 검증)**
**위치**: `src/validation/enhanced_validation.py` → `bootstrap_validation()`

**내용**:
- 블록 부트스트랩 (20일 블록)
- 1000회 반복 샘플링
- 시계열 의존성 보존

**지표**:
- CAGR (연평균 수익률) 분포 및 신뢰구간
- MDD (최대 낙폭) 분포 및 신뢰구간
- 최소 K-ICS 분포 및 신뢰구간

**목적**: 통계적 불확실성 정량화

---

### 4. **RCR Metrics (Risk-Cost Ratio)**
**위치**: `src/validation/enhanced_validation.py` → `calculate_rcr_metrics()`

**내용**:
- RCR = (K-ICS 개선량) / (헤지 비용)
- 1원의 비용으로 얼마의 자본을 절감했는지 측정

**지표**:
- 평균/중앙값/표준편차 RCR
- 최소/최대 RCR 범위

**목적**: 비용 대비 효과 측정

---

### 5. **K-ICS Quantile Analysis (분위수 분석)**
**위치**: `src/validation/enhanced_validation.py` → `kics_quantile_analysis()`

**내용**:
- 하위 분위수 분석 (1%, 5%, 10%, 25%)
- 위험 구간(100% 미만)에서의 방어력 확인

**지표**:
- 각 분위수별 K-ICS 값
- 100% 미만 일수 및 비율

**목적**: Tail Risk에서의 방어력 확인

---

## 🔮 추가로 고려할 수 있는 검증론

### 6. **Monte Carlo Simulation (몬테카를로 시뮬레이션)**
**목적**: 확률적 시나리오 생성 및 분포 추정

**방법**:
- 수천~수만 개의 랜덤 시나리오 생성
- 각 시나리오에서 K-ICS 분포 추정
- VaR, CVaR 계산

**장점**:
- 다양한 시나리오 커버
- 확률적 분포 정량화

**구현 예시**:
```python
def monte_carlo_validation(n_simulations=10000):
    kics_results = []
    for _ in range(n_simulations):
        # 랜덤 시장 데이터 생성
        scenario = generate_random_scenario()
        kics = run_simulation(scenario)
        kics_results.append(kics)
    
    # VaR, CVaR 계산
    var_95 = np.percentile(kics_results, 5)
    cvar_95 = np.mean(kics_results[kics_results <= var_95])
    return var_95, cvar_95
```

---

### 7. **Historical Backtesting (역사적 백테스팅)**
**목적**: 과거 실제 시장 데이터로 검증

**방법**:
- 실제 과거 위기 구간 선택
  - 2008 금융위기
  - 2011 유럽 재정위기
  - 2015 중국 주식시장 폭락
  - 2020 COVID-19
- 각 구간에서 모델 성능 평가

**장점**:
- 실제 시장 상황 반영
- 검증 신뢰도 향상

**구현 예시**:
```python
def historical_backtest(crisis_periods):
    results = {}
    for period_name, start_date, end_date in crisis_periods:
        data = load_historical_data(start_date, end_date)
        kics = run_simulation(data)
        results[period_name] = {
            'min_kics': np.min(kics),
            'avg_kics': np.mean(kics),
            'below_100_days': np.sum(kics < 100)
        }
    return results
```

---

### 8. **Sensitivity Analysis (민감도 분석)**
**목적**: 각 변수의 변화가 K-ICS에 미치는 영향 측정

**방법**:
- 각 변수(VIX, Correlation, 금리 등)를 ±10%, ±20% 변화
- K-ICS 변화량 측정
- 가장 민감한 변수 식별

**지표**:
- 각 변수별 K-ICS 변화율
- 상관계수

**구현 예시**:
```python
def sensitivity_analysis(base_scenario):
    variables = ['VIX', 'Correlation', 'US_10Y', 'KR_10Y']
    results = {}
    
    for var in variables:
        for change in [-0.2, -0.1, 0.1, 0.2]:
            modified = base_scenario.copy()
            modified[var] *= (1 + change)
            kics = run_simulation(modified)
            results[f'{var}_{change}'] = np.mean(kics)
    
    return results
```

---

### 9. **VaR/CVaR (Value at Risk / Conditional VaR)**
**목적**: 위험 한도 정량화

**방법**:
- VaR: 특정 확률(예: 5%)에서의 최악 K-ICS
- CVaR: VaR 이하 구간의 평균 K-ICS

**지표**:
- VaR_95, VaR_99
- CVaR_95, CVaR_99

**구현 예시**:
```python
def calculate_var_cvar(kics_ratios, confidence=0.95):
    var = np.percentile(kics_ratios, (1 - confidence) * 100)
    cvar = np.mean(kics_ratios[kics_ratios <= var])
    return var, cvar
```

---

### 10. **Regime-based Validation (국면별 검증)**
**목적**: 시장 국면(Normal/Transition/Panic)별 성능 평가

**방법**:
- HMM으로 국면 분류
- 각 국면별 K-ICS 통계
- 국면 전이 시 성능 변화

**지표**:
- Normal/Transition/Panic별 평균 K-ICS
- 국면 전이 시 K-ICS 변화율

**구현 예시**:
```python
def regime_based_validation(data, regime_classifier):
    regimes = regime_classifier.predict(data)
    results = {}
    
    for regime in ['Normal', 'Transition', 'Panic']:
        mask = regimes == regime
        kics = run_simulation(data[mask])
        results[regime] = {
            'avg_kics': np.mean(kics),
            'min_kics': np.min(kics),
            'below_100_pct': np.sum(kics < 100) / len(kics) * 100
        }
    
    return results
```

---

### 11. **Out-of-Sample Testing (샘플 외 검증)**
**목적**: 학습 데이터와 완전히 분리된 데이터로 검증

**방법**:
- 학습 기간: 2010-2020
- 검증 기간: 2021-2024 (완전히 분리)
- 시간 순서 보장

**장점**:
- 과적합 방지
- 실제 성능 추정

---

### 12. **Cross-Validation (시계열용)**
**목적**: 시계열 데이터에서 K-Fold 대신 Time Series Split

**방법**:
- TimeSeriesSplit 사용
- 각 Fold에서 Train/Test 분리
- 평균 성능 계산

**구현 예시**:
```python
from sklearn.model_selection import TimeSeriesSplit

def time_series_cv(data, n_splits=5):
    tscv = TimeSeriesSplit(n_splits=n_splits)
    results = []
    
    for train_idx, test_idx in tscv.split(data):
        train_data = data.iloc[train_idx]
        test_data = data.iloc[test_idx]
        
        # 모델 학습 및 평가
        kics = evaluate_model(train_data, test_data)
        results.append(kics)
    
    return np.mean(results), np.std(results)
```

---

### 13. **Scenario Analysis (시나리오 분석)**
**목적**: 특정 가정 하에서의 결과 분석

**방법**:
- Best Case / Base Case / Worst Case 시나리오
- 각 시나리오별 K-ICS 예측
- 시나리오별 확률 가중 평균

**지표**:
- 각 시나리오별 K-ICS
- 가중 평균 K-ICS

---

### 14. **Tail Risk Metrics (꼬리 위험 지표)**
**목적**: 극단적 사건에서의 성능 측정

**방법**:
- 1% 최악 시나리오 선택
- Tail Ratio = (평균 K-ICS) / (1% 최악 K-ICS)
- Tail Risk Score 계산

**지표**:
- Tail Ratio
- Tail Risk Score
- 최악 1% 평균 K-ICS

---

### 15. **Robustness Testing (견고성 테스트)**
**목적**: 모델 파라미터 변화에 대한 견고성 확인

**방법**:
- 학습률, 배치 크기 등 하이퍼파라미터 변화
- 각 설정에서 성능 비교
- 최적 설정 식별

**지표**:
- 각 설정별 평균 K-ICS
- 표준편차 (낮을수록 견고)

---

## 📈 검증론 우선순위 추천

### 높은 우선순위 (즉시 구현 권장)
1. ✅ **Stress Testing** (이미 구현)
2. ✅ **Walk-Forward Validation** (이미 구현)
3. ✅ **Bootstrap Validation** (이미 구현)
4. **Monte Carlo Simulation** - 다양한 시나리오 커버
5. **Historical Backtesting** - 실제 위기 구간 검증

### 중간 우선순위 (추가 개선 시)
6. **Sensitivity Analysis** - 변수별 영향 측정
7. **VaR/CVaR** - 위험 한도 정량화
8. **Regime-based Validation** - 국면별 성능 평가

### 낮은 우선순위 (선택적)
9. **Out-of-Sample Testing** - 이미 Train/Test 분리됨
10. **Cross-Validation** - Walk-Forward와 유사
11. **Scenario Analysis** - Stress Testing과 유사
12. **Tail Risk Metrics** - Quantile Analysis와 유사
13. **Robustness Testing** - 하이퍼파라미터 튜닝 시

---

## 🎯 현재 검증론의 강점

1. **체계적**: 7개 스트레스 시나리오로 다양한 위험 커버
2. **통계적**: Bootstrap으로 불확실성 정량화
3. **시간 순서 보장**: Walk-Forward로 Look-ahead bias 방지
4. **비용 효율**: RCR로 비용 대비 효과 측정
5. **Tail Risk**: Quantile Analysis로 극단 상황 확인

---

## 💡 추가 개선 제안

1. **Monte Carlo Simulation 추가**
   - 10,000개 랜덤 시나리오 생성
   - VaR/CVaR 계산

2. **Historical Backtesting 추가**
   - 실제 위기 구간 데이터 수집
   - 각 위기별 성능 평가

3. **Sensitivity Analysis 추가**
   - 각 변수별 K-ICS 변화율 측정
   - 가장 민감한 변수 식별

4. **시각화 강화**
   - 각 검증론별 그래프 생성
   - 대시보드 형태로 통합

---

## 📝 참고 자료

- **Stress Testing**: Basel III, Solvency II 규제 요구사항
- **Walk-Forward**: 시계열 머신러닝 표준 기법
- **Bootstrap**: 통계적 추론 표준 방법
- **VaR/CVaR**: 금융 리스크 관리 표준 지표


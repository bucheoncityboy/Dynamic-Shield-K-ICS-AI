# 파일별 역할 상세 설명

## 📁 프로젝트 구조

```
Dynamic-Shield-K-ICS-AI/
├── DATA/                          # 데이터 디렉토리
│   ├── data/                      # 실제 시장 데이터
│   └── synthetic_stress/          # 합성 스트레스 시나리오
├── src/
│   ├── core/                      # 핵심 엔진 모듈
│   └── validation/                # 검증 및 분석 모듈
└── 문서 및 설정 파일
```

---

## 📂 DATA 디렉토리

### `DATA/data/Dynamic_Shield_Data_v4.csv`
**역할**: 실제 시장 데이터 (5,292일)

**내용**:
- **날짜**: 2010-01-04 ~ 2024-12-30
- **주요 컬럼**:
  - `SPX`: S&P 500 지수
  - `FX`: KRW/USD 환율
  - `KOSPI`: KOSPI 지수
  - `VIX`: 변동성 지수
  - `US_10Y`, `KR_10Y`: 미국/한국 10년 국채 금리
  - `Yield_Spread`: KR_10Y - US_10Y (CDS 프록시)
  - `Swap_Point_Proxy`: 스왑포인트 대용치
  - `FX_MA_Divergence`: 환율 이동평균 이탈
  - `Correlation`: 주식-환율 상관계수

**용도**:
- RL 학습 데이터 (70% train, 30% test)
- 백테스팅 데이터
- 검증 데이터

---

### `DATA/synthetic_stress/*.csv`
**역할**: 합성 스트레스 시나리오 (7개)

**시나리오 목록**:

1. **Scenario_A_Stagflation.csv** (253일)
   - 스태그플레이션: 인플레이션 + 경기침체
   - VIX 상승, 금리 상승, 주가 하락

2. **Scenario_B_Correlation_Breakdown.csv** (253일)
   - 상관관계 붕괴: Natural Hedge 실패
   - Correlation이 음수에서 양수로 전환

3. **Scenario_COVID19.csv** (123일)
   - COVID-19 팬데믹 시뮬레이션
   - 급격한 VIX 상승, 주가 폭락

4. **Scenario_Tail_Risk.csv** (10,001일)
   - 장기 꼬리 위험 시나리오
   - 다양한 극단 상황 포함

5. **Scenario_C_Interest_Rate_Shock.csv** (250일)
   - 금리 쇼크: 국고채 10Y 급등 (3.37% → 5.5%)
   - 주가/환율은 중립 유지

6. **Scenario_D_Swap_Point_Extreme.csv** (250일)
   - 스왑포인트 극단 마이너스
   - 헤지 비용 극대화 상황

7. **Scenario_E_Regime_Transition.csv** (250일)
   - Regime 전이: Normal → Transition → Panic
   - VIX 점진적 상승 (15 → 35)

**용도**:
- 스트레스 테스트
- 극단 상황에서의 K-ICS 방어력 검증

---

## 🔧 src/core/ - 핵심 엔진 모듈

### `kics_real.py`
**역할**: K-ICS 계산 엔진 (Ground Truth)

**주요 클래스**:

1. **`KICSCalculator`**
   - 실시간 K-ICS 비율 계산
   - 자산/부채 가치 갱신
   - 규제 충격 시나리오 적용
   - 일일 단위 업데이트

2. **`RatioKICSEngine`**
   - 배치 SCR 비율 계산
   - 여러 헤지 비율 동시 계산
   - 상관계수 반영

**핵심 메서드**:
- `update_and_calculate()`: 자산/부채 갱신 후 K-ICS 계산
- `calculate_scr_ratio_batch()`: 배치 SCR 계산

**용도**:
- 실제 K-ICS 계산 (규제 기준)
- Surrogate 모델 학습 데이터 생성
- 검증 시 Ground Truth

---

### `kics_surrogate.py`
**역할**: K-ICS 계산 근사 모델 (AI Surrogate)

**주요 기능**:
- MLP Regressor로 K-ICS 계산 근사
- 학습 데이터: `RatioKICSEngine`으로 생성
- 목표 오차: < 1% (실제로 0.05% 달성)

**핵심 메서드**:
- `train_surrogate_model()`: Surrogate 모델 학습
- `RobustSurrogate.predict()`: 빠른 추론

**용도**:
- RL 학습 시 빠른 K-ICS 계산
- 실시간 추론 (마이크로초 단위)
- 대규모 시뮬레이션

**성능**:
- MAPE: 0.0518%
- 추론 속도: 실시간 가능

---

### `regime.py`
**역할**: 시장 국면 탐지 (Hidden Markov Model)

**주요 클래스**: `RegimeClassifier`

**기능**:
- HMM으로 3개 시장 국면 분류:
  - **Normal**: 안정적 시장 (Correlation 음수)
  - **Transition**: 전환 구간
  - **Panic**: 패닉 상황 (Correlation 양수)
- 특징: VIX, VIX_Change, FX_MA_Divergence, Yield_Spread

**핵심 메서드**:
- `fit()`: HMM 모델 학습
- `predict()`: 현재 시장 국면 예측

**용도**:
- 시장 상황 인식
- 국면별 최적 헤지 비율 결정
- Risk Paradox 활용 시점 판단

---

### `realistic_data.py`
**역할**: 현실적인 시장 데이터 생성 및 로드

**주요 기능**:
1. **실제 데이터 로드**
   - `Dynamic_Shield_Data_v4.csv` 로드
   - Train/Test 분리 (70% / 30%)
   - 시간 순서 보장 (Anti-Overfitting)

2. **합성 데이터 생성**
   - GARCH 변동성 모델
   - Fat-tail 분포
   - Markov chain 기반 Regime 전환

**핵심 함수**:
- `load_real_data_for_training()`: 학습용 데이터
- `load_real_data_for_testing()`: 테스트용 데이터
- `generate_market_scenario()`: 합성 시나리오 생성

**용도**:
- RL 학습 데이터 제공
- 백테스팅 데이터 제공
- 데이터 증강

---

### `environment.py`
**역할**: RL 환경 (초기 버전)

**주요 클래스**: `KICSEnvironment`

**기능**:
- State Space: [hedge_ratio, VIX, correlation, SCR_ratio]
- Action Space: 헤지 비율 조정
- Reward Function: K-ICS 방어 + 비용 최소화

**용도**:
- 초기 RL 환경 (현재는 `gym_environment.py` 사용)

---

### `gym_environment.py`
**역할**: Gymnasium 호환 RL 환경 (현재 사용 중)

**주요 클래스**: `KICSGymEnv`

**기능**:
- OpenAI Gym/Gymnasium 호환
- Stable-Baselines3와 통합
- 실제 데이터 기반 시장 시뮬레이션

**State Space**:
- `[hedge_ratio, vix_norm, corr_norm, scr_ratio]`

**Action Space**:
- Continuous: [-1, 1] → 헤지 비율 변화 [-0.1, 0.1]

**Reward Function**:
- Capital Efficiency - Hedge Cost - Transaction Cost - KICS Penalty
- K-ICS < 100% → -1000 페널티

**용도**:
- PPO 학습 환경
- 모델 평가 환경

---

### `agent.py`
**역할**: Dynamic Shield 에이전트

**주요 클래스**: `DynamicShieldAgent`

**기능**:
- Safety Layer 구현
- VIX 기반 점진적 De-risking
- 급격한 변화 방지

**핵심 메서드**:
- `decide_hedge_ratio()`: 헤지 비율 결정
- `apply_safety_layer()`: Safety Layer 적용

**용도**:
- Safety Layer 콜백
- 룰 기반 전략 (AI 모델 없을 때)

---

### `ppo_trainer.py`
**역할**: PPO 학습 매니저

**주요 클래스**: `PPOTrainer`

**기능**:
- PPO 알고리즘 설정 및 학습
- Callback 관리 (Reward Logging, Safety Layer)
- 모델 저장/로드
- 학습 결과 평가 및 시각화

**핵심 메서드**:
- `setup()`: 환경 및 모델 초기화
- `train()`: PPO 학습 실행
- `evaluate()`: 학습된 모델 평가
- `save()`: 모델 저장 (`ppo_kics.zip`)

**용도**:
- RL 모델 학습
- 하이퍼파라미터 튜닝
- 학습 진행 상황 모니터링

---

### `system.py`
**역할**: 통합 시스템 (Dynamic Shield System)

**주요 클래스**: `DynamicShieldSystem`

**기능**:
- K-ICS Calculator + Regime Classifier 통합
- 실제 데이터 로드 및 전처리
- 일일 시뮬레이션 (`step()`)
- 상태 벡터 생성 (`_get_observation()`)

**핵심 메서드**:
- `step()`: 하루 진행 (시장 데이터 업데이트, K-ICS 계산)
- `_get_observation()`: 상태 벡터 생성

**용도**:
- 전체 시스템 시뮬레이션
- 백테스팅
- 검증

---

## ✅ src/validation/ - 검증 및 분석 모듈

### `data_sanity_check.py`
**역할**: 데이터 무결성 및 누수 검사

**주요 기능**:
1. **Yield Spread 일관성 검사**
   - `Yield_Spread = KR_10Y - US_10Y` 확인

2. **결측치 및 전처리 검사**
   - 핵심 컬럼 결측치 확인
   - 날짜 인덱스 단조 증가 확인

3. **Train/Test 분리 검증**
   - 시간 기반 분리 확인
   - 겹침 방지 확인

4. **수작업 Cross-Check**
   - 특정 날짜의 원본 CSV ↔ 상태 벡터 추적

**용도**:
- 데이터 전처리 검증
- 누수/오버피팅 방지
- 데이터 품질 확인

---

### `enhanced_validation.py`
**역할**: 강화된 검증 프레임워크

**주요 클래스**: `EnhancedValidator`

**검증 방법**:
1. **Walk-Forward Validation**
   - 롤링 윈도우 방식
   - Look-ahead bias 방지

2. **Bootstrap Validation**
   - 블록 부트스트랩
   - 통계적 불확실성 정량화

3. **RCR Metrics**
   - Risk-Cost Ratio 계산
   - 비용 대비 효과 측정

4. **K-ICS Quantile Analysis**
   - 하위 분위수 분석
   - Tail Risk 확인

**용도**:
- 고급 검증 기법 적용
- 통계적 신뢰도 향상

---

### `run_stress_validation.py`
**역할**: 스트레스 시나리오 통합 검증

**주요 기능**:
1. **7개 스트레스 시나리오 검증**
   - 각 시나리오별 K-ICS 통계
   - AI 모델 또는 룰 기반 전략 사용

2. **AI 모델 통합**
   - PPO 모델 자동 로드
   - 모델 없을 시 룰 기반 폴백

3. **Enhanced Validation 통합**
   - Walk-Forward, Bootstrap 등 자동 실행

**핵심 함수**:
- `run_scenario_validation()`: 단일 시나리오 검증
- `run_all_stress_validations()`: 모든 시나리오 검증

**용도**:
- 스트레스 테스트 통합 실행
- 극단 상황 검증
- 종합 리포트 생성

---

### `stress_scenario_generator.py`
**역할**: 추가 스트레스 시나리오 생성

**주요 클래스**: `StressScenarioGenerator`

**생성 시나리오**:
1. **Interest Rate Shock**: 금리 쇼크
2. **Swap Point Extreme**: 스왑포인트 극단
3. **Regime Transition**: Regime 전이

**핵심 메서드**:
- `generate_interest_rate_shock()`
- `generate_swap_point_extreme()`
- `generate_regime_transition()`

**용도**:
- 추가 스트레스 시나리오 생성
- 특정 위험 요소 검증

---

### `backtest.py`
**역할**: 백테스팅 엔진

**주요 클래스**: `BacktestEngine`

**기능**:
- 여러 전략 비교 (100% Hedge, 80% Fixed, Rule-based, Dynamic Shield)
- AI 모델 추론 통합
- 성과 지표 계산 (CAGR, Sharpe, MDD, RCR)

**핵심 메서드**:
- `run_backtest()`: 백테스팅 실행
- `strategy_real_ai_inference()`: AI 추론

**용도**:
- 전략 성과 비교
- AI 모델 평가
- 성과 리포트 생성

---

### `solvency_visualizer.py`
**역할**: K-ICS 방어력 시각화

**기능**:
- COVID-19 등 위기 구간에서의 K-ICS 추이 시각화
- 여러 전략 비교
- Solvency 방어력 확인

**용도**:
- 위기 상황 분석
- 시각적 리포트 생성

---

### `proof_risk_paradox.py`
**역할**: Risk Paradox 수학적 증명

**기능**:
- 100% 헤지가 최선이 아님을 수학적으로 증명
- Natural Hedge 효과 정량화
- 상관계수별 최적 헤지 비율 계산

**용도**:
- 핵심 발견 증명
- 이론적 근거 제시

---

### `shap_analysis.py`
**역할**: SHAP 분석 (XAI)

**기능**:
- AI 모델의 의사결정 설명
- 각 변수(VIX, Correlation 등)의 기여도 측정
- "왜 100% 헤지가 아닌가?" 설명

**용도**:
- 모델 해석 가능성
- 의사결정 근거 제시

---

### `stress_safety.py`
**역할**: Safety Layer 스트레스 테스트

**기능**:
- VIX > 40 주입 테스트
- Emergency De-risking 트리거 확인
- Safety Layer 동작 검증

**용도**:
- Safety Layer 검증
- 극단 상황 대응 확인

---

### `advanced_viz.py`
**역할**: 고급 시각화

**기능**:
- Efficient Frontier 시각화
- Risk-Cost 트레이드오프
- 전략 비교 차트

**용도**:
- 시각적 분석
- 리포트 생성

---

## 📄 루트 디렉토리 파일

### `README.md`
**역할**: 프로젝트 전체 설명서

**내용**:
- 프로젝트 개요
- 주요 결과
- 설치 및 실행 방법
- 파일 구조

---

### `설명.md`
**역할**: 프로젝트 상세 설명

**내용**:
- 프로젝트 배경
- 기술적 세부사항
- 구현 철학

---

### `requirements.txt`
**역할**: Python 패키지 의존성

**주요 패키지**:
- `stable-baselines3`: RL 라이브러리
- `gymnasium`: RL 환경
- `scikit-learn`: 머신러닝
- `pandas`, `numpy`: 데이터 처리
- `matplotlib`: 시각화

---

### `.gitignore`
**역할**: Git 추적 제외 파일 목록

**제외 항목**:
- Python 캐시
- 가상환경
- 모델 파일 (`*.zip`, `*.pkl`)
- 데이터 파일 (`*.csv`)
- PDF 파일
- 로그 파일

---

## 🔄 파일 간 상호작용

### 학습 파이프라인
```
realistic_data.py (데이터 로드)
    ↓
gym_environment.py (RL 환경)
    ↓
ppo_trainer.py (PPO 학습)
    ↓
ppo_kics.zip (학습된 모델)
```

### 검증 파이프라인
```
run_stress_validation.py
    ↓
stress_scenario_generator.py (시나리오 생성)
    ↓
kics_real.py (K-ICS 계산)
    ↓
ppo_kics.zip (AI 추론)
    ↓
enhanced_validation.py (고급 검증)
```

### 백테스팅 파이프라인
```
backtest.py
    ↓
system.py (통합 시스템)
    ↓
regime.py (국면 탐지)
    ↓
kics_real.py (K-ICS 계산)
```

---

## 📊 데이터 흐름

### 학습 시
```
Dynamic_Shield_Data_v4.csv (70%)
    ↓
realistic_data.py (Train 데이터)
    ↓
gym_environment.py (환경)
    ↓
ppo_trainer.py (학습)
    ↓
ppo_kics.zip (모델 저장)
```

### 검증 시
```
스트레스 시나리오 CSV
    ↓
run_stress_validation.py
    ↓
ppo_kics.zip (AI 추론)
    ↓
kics_real.py (K-ICS 계산)
    ↓
결과 리포트
```

---

## 🎯 핵심 파일 요약

| 파일 | 역할 | 중요도 |
|------|------|--------|
| `kics_real.py` | K-ICS 계산 (Ground Truth) | ⭐⭐⭐⭐⭐ |
| `gym_environment.py` | RL 환경 | ⭐⭐⭐⭐⭐ |
| `ppo_trainer.py` | PPO 학습 | ⭐⭐⭐⭐⭐ |
| `system.py` | 통합 시스템 | ⭐⭐⭐⭐ |
| `regime.py` | 시장 국면 탐지 | ⭐⭐⭐⭐ |
| `run_stress_validation.py` | 스트레스 검증 | ⭐⭐⭐⭐ |
| `backtest.py` | 백테스팅 | ⭐⭐⭐ |
| `enhanced_validation.py` | 고급 검증 | ⭐⭐⭐ |

---

## 💡 사용 시나리오별 파일 매핑

### 1. RL 학습 실행
```
ppo_trainer.py
    → gym_environment.py
    → realistic_data.py
    → kics_surrogate.py (선택)
```

### 2. 스트레스 테스트
```
run_stress_validation.py
    → stress_scenario_generator.py
    → kics_real.py
    → ppo_kics.zip (AI 모델)
```

### 3. 백테스팅
```
backtest.py
    → system.py
    → regime.py
    → kics_real.py
```

### 4. 데이터 검증
```
data_sanity_check.py
    → realistic_data.py
    → system.py
```

---

이 문서는 프로젝트의 각 파일이 어떤 역할을 하는지 상세히 설명합니다. 특정 파일에 대해 더 자세한 정보가 필요하면 알려주세요!


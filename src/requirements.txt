# Dynamic Shield Project
# Python 3.11 Required (3.12+ may have compatibility issues)
# 
# Installation:
#   conda create -n quant python=3.11 pytorch cpuonly -c pytorch -y
#   conda activate quant
#   pip install -r requirements.txt
#   conda install -c conda-forge hmmlearn -y

# Core Libraries
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
scipy>=1.7.0

# Machine Learning
scikit-learn>=1.0.0
hmmlearn>=0.2.7  # Install via conda: conda install -c conda-forge hmmlearn

# Reinforcement Learning
gymnasium>=0.28.0
stable-baselines3>=2.0.0
torch>=2.0.0  # Install via conda: conda install pytorch cpuonly -c pytorch

# Utilities
tqdm>=4.62.0
rich>=13.0.0  # For PPO progress bar

# Development (Optional)
ipykernel>=6.0.0
tensorboard>=2.10.0

# XAI (Optional)
# shap>=0.42.0  # Uncomment if needed